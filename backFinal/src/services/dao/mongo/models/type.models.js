const strUniqueReq = {
    type: String,
    unique: true,
    required: true
};

const strReq = {
    type: String,
    required: true
};

const str = {
    type: String
};

const num = {
    type: Number,
};
const numReq = {
    type: Number,
    required: true
};

const numUniqReq = {
    type: Number,
    unique: true,
    required: true
};
